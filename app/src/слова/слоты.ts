import type { СостояниеСлота } from '@/domain/slotStatus';

/* Как называются слоты, пути данных и состояния настройки (SugarLife#324).

   Слово «слот» человеку не говорят: он видит «Сахар» и «Подача инсулина». Слово «direct»
   — тем более. Всё это переводы состояния в речь, и жили они в трёх доменных модулях
   сразу: подпись слота в slotStatus, имя слота в sources, слот железки в nearby. Один
   вопрос — три места, и в каждом свой вариант ответа.

   Полнота держится типом: состояние без подписи не соберётся. */

/** Что человек делает с этим слотом дальше — коротко, для строки списка. */
export const ПОДПИСЬ_СЛОТА: Record<СостояниеСлота, string> = {
  'нет записи': 'настроить',
  'без модели': 'только через облако',
  'нужен мост': 'записано — нужен мост',
  'настроено': 'настроено',
  'на связи': 'на связи',
};

/* Каким путём приходят данные. «Прямое чтение» и «через мост» — разные вещи для
   человека: во втором случае между телефоном и прибором стоит ещё одна коробка, которая
   тоже может сесть или потеряться. */
export const СЛОВО_ПУТИ: Record<'direct' | 'bridged' | 'cloud', string> = {
  direct: 'прямое чтение', bridged: 'через мост', cloud: 'через облако',
};


/* То же самое строчными и по роли железки: «в слоте сахара», «в слоте подачи инсулина».
   Не сокращаем до «инсулин»: слот отвечает за подачу, а инсулин бывает и в ручке. */
export const СЛОТ_ЖЕЛЕЗКИ: Record<'cgm' | 'insulin', string> = {
  cgm: 'сахар', insulin: 'подача инсулина',
};
