import type { Пустота } from '@/domain/scanReadiness';

/* СЛОВА ПУСТОГО ПОИСКА (#324, переехало из domain/scanReadiness.ts).

   Состояние считает домен: пустой ли эфир, идёт ли скан, давно ли ждём. Как это назвать —
   здесь, и разница между четырьмя фразами не косметическая: это разница между «жди» и «иди
   чини», то есть между спокойствием и суетой.

   Словарь полон по типу: новое состояние не соберётся, пока ему не написана фраза. Тот же приём,
   что у находок разбора (`показ/находки.ts`), и по той же причине — потерять состояние молча тут
   означает пустой экран без объяснения, а именно из-за такого экрана задача #333 и заведена. */
export const СЛОВО_ПУСТОТЫ: Record<Пустота, string> = {
  'эфира нет': 'В браузере блютуса нет — это свойство браузера, а не настроек. Приборы видны в '
    + 'приложении на телефоне; данные при этом идут через облако, и всё остальное работает.',
  'поиск не идёт': 'Поиск не идёт. Обычно это значит, что приложению не разрешили блютус.',
  'слушаю': 'Слушаю эфир. Реклама прибора приходит раз в несколько секунд — держите его ближе к '
    + 'телефону.',
  /* Причины по убыванию вероятности, а не по удобству перечисления: первой — та, что случается
     чаще всего и человеком не подозревается вовсе. */
  'долго пусто': 'Пока никого. Чаще всего причина одна: прибор уже занят другим телефоном или '
    + 'родным приложением производителя — у сенсора один хозяин за раз. Вторая по частоте: прибор '
    + 'далеко или спит между обменами. И третья: он не в режиме поиска — новый сенсор надо '
    + 'активировать, помпу разбудить.',
};

export const словоПустоты = (п: Пустота): string => СЛОВО_ПУСТОТЫ[п];
