import type { СостояниеУчётки } from '@/domain/cloudAccounts';

/* СЛОВА ПРО ВЕНДОРСКУЮ УЧЁТКУ (#324, переехало из domain/cloudAccounts.ts).

   Главное правило этого места живёт в домене и остаётся там: при ошибке показываем remediation
   движка КАК ЕСТЬ и своим текстом не подменяем. LibreLinkUp живёт в двенадцати регионах, и вход не
   в тот регион возвращает не «неверный пароль», а указание «учётка в регионе EU2». Напишем от себя
   «проверьте пароль» — человек пойдёт менять верный пароль и не войдёт никогда.

   Здесь только четыре наших слова: те, где движку сказать нечего. */
export const СЛОВО_УЧЁТКИ: Record<СостояниеУчётки, string> = {
  вошли: 'подключено',
  входим: 'входим…',
  ошибка: 'не удалось войти',
  неизвестно: 'состояние неизвестно',
};
