import type { Пустой } from '@/domain/deviceLog';

/* ПОЧЕМУ ЖУРНАЛ ОБМЕНА ПУСТ — СЛОВАМИ (#324, переехало из domain/deviceLog.ts).

   Четыре состояния, и у каждого свой выход: обновить приложение, открыть на телефоне, поднять
   подробность, подождать. Общая фраза «записей нет» не отвечает никому из четверых.

   Словарь полон по типу: новое состояние не соберётся, пока ему не написана фраза. */
export const СЛОВО_ПУСТОГО: Record<Пустой, string> = {
  /* На телефоне «журнала нет» означает, что установленная сборка старше журнала, и лечится это
     обновлением приложения, а не переходом куда-то. */
  'сборка старая': 'Эта сборка приложения журнал ещё не ведёт. Он появится в следующей — '
    + 'скачайте свежую в «О приложении».',
  /* А в браузере журнала нет по построению: его ведёт движок, которого там не существует. */
  'в браузере': 'Журнал обмена ведёт движок приложения, а в браузере его нет. Откройте раздел в '
    + 'приложении на телефоне.',
  'только служебные': 'Есть только служебные записи. Включите подробный обмен — тогда будет '
    + 'видно каждый кадр.',
  'записей нет': 'Записей пока нет. Они появятся, как только приложение заговорит с прибором.',
};

export const словоПустого = (п: Пустой): string => СЛОВО_ПУСТОГО[п];
