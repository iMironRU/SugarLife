import type { ComponentType } from 'react';

/* ТОЧКА ВХОДА В PRO — единственная (SugarLife#296).

   Всё, что есть только в Pro, лежит в этом каталоге и выходит наружу через этот файл. Тогда издание
   исключается из Lite одной веткой в `src/издание.ts`, а не выковыриванием кусков из общих экранов.

   Пока список пуст по существу: пульт помпы — остановить и возобновить подачу, часы, базальный
   профиль — ждёт флагов умений от ядра (`canSuspendResume`, `canSetTime`, `canWriteBasalProfile`,
   SugarLifeCore#59/#60). Каталог заведён раньше экранов намеренно: границу дешевле проложить до того,
   как по обе стороны от неё появится код.

   ПРАВИЛО ДЛЯ ВСЕГО, ЧТО СЮДА ЛЯЖЕТ. Раздел Pro не имеет права быть кнопкой внутри общего экрана:
   тогда его не исключить, не тронув общий экран. Он приходит целым разделом со своей точкой входа, а
   общий экран спрашивает только «есть ли он». */

export interface РазделPro {
  /** Метка для стека страниц: по ней раздел собирается заново после перезапуска. */
  id: string;
  название: string;
  /** Что открыть. Ленивый компонент — грузится при первом заходе, а не при старте. */
  Экран: ComponentType<{ onClose: () => void }>;
}

/** Разделы, которые есть только в Pro-сборке. Пустой список — законное состояние. */
export const РАЗДЕЛЫ_PRO: РазделPro[] = [];
